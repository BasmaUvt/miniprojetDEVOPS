package com.example.tunisavia.entity

class TeckParam {
    var id: Int? = -1
    var isready: Boolean? = false

    override fun toString(): String {
        return "TeckParam(id=$id, isready=$isready)"
    }
}